<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
	<link rel="stylesheet" href="style.css">
    <title>Assignment Grades</title>
</head>
<body>
    <h1>Assignments</h1>

    	<table>
		<thead>
			<tr>
				<th>Assignment</th>
				<th>Score</th>
				<th>Total</th>
                <th>Completed</th>
                <th>Actions</th>
			</tr>
		</thead>
		<tbody>
		{{#each assignmentArray}}
			<tr>
				<td>{{name}}</td>
				<td>{{score}}</td>
                <td>{{total}}</td>
                <td>{{#if completed}} &check; {{/if}}</td>
				<td>
					<a href="/api/assignments/{{id}}/edit">Edit</a> |
					<a href="/api/assignments/{{id}}/delete">Delete</a> 

				</td>
			</tr>
		{{/each}}
		</tbody>
	</table>
    <p>{{#if showAverage}} Overall Average: {{average}} {{/if}}</p>
    <a href="add">Add an Assignment</a>
    
</body>
</html>